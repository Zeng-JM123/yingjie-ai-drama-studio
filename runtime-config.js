/* Public GitHub Pages configuration. Never put a secret in this file. */
window.YINGJIE_CONFIG = {
  // Leave blank until the HTTPS gateway is deployed. On localhost the app safely
  // falls back to http://127.0.0.1:8787; a published site must use an HTTPS URL.
  // Never add secrets here.
  studioApiBaseUrl: "",
  videoApiBaseUrl: "",
  projectId: "yesterday-signal-ep01"
};
