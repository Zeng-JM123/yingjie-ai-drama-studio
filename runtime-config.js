/* Public GitHub Pages configuration. Never put a secret in this file. */
window.YINGJIE_CONFIG = {
  // Set this to the public HTTPS URL of the deployed studio service. Never add secrets here.
  // When studioApiBaseUrl is empty, videoApiBaseUrl is used for both project persistence and video tasks.
  studioApiBaseUrl: "",
  videoApiBaseUrl: "",
  projectId: "yesterday-signal-ep01"
};
